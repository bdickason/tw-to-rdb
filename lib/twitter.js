// Generated by CoffeeScript 1.4.0

/* Handle integration with Twitter API
*/


(function() {
  var OAuth, Twitter;

  OAuth = (require('oauth')).OAuth;

  exports.Twitter = Twitter = (function() {

    function Twitter(cfg, redis) {
      var oa;
      this.cfg = cfg;
      this.redis = redis;
      this.oa = oa = new OAuth('request_url', 'access_url', this.cfg.TW_CONSUMER_KEY, this.cfg.TW_CONSUMER_SECRET, '1.0', 'http://localhost:3000/tw/callback', 'HMAC-SHA1');
    }

    Twitter.prototype.getFavorites = function(count, callback) {
      var _this = this;
      return this.redis.hgetall("user:" + this.cfg.TW_USERNAME + ":Twitter", function(error, reply) {
        if (error) {
          return console.log(error);
        } else {
          return _this.oa.getProtectedResource('https://api.twitter.com/1.1/favorites/list.json?count=#{count}', 'GET', reply.access_token, reply.access_token_secret, function(error, data, response) {
            if (error) {
              return callback('Error: getting OAuth resource: ' + error);
            } else {
              return callback(JSON.parse(data));
            }
          });
        }
      });
    };

    return Twitter;

  })();

}).call(this);
