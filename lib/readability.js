// Generated by CoffeeScript 1.4.0

/* Handle integration with Readability API
*/


(function() {
  var OAuth, Readability, cfg;

  cfg = require('../cfg/config.js');

  OAuth = (require('oauth')).OAuth;

  exports.Readability = Readability = (function() {

    function Readability() {
      var oa;
      this.oa = oa = new OAuth('https://www.readability.com/api/rest/v1/oauth/request_token/', 'https://www.readability.com/api/rest/v1/oauth/access_token/', cfg.RDB_CONSUMER_KEY, cfg.RDB_CONSUMER_SECRET, '1.0', 'http://localhost:3000/rdb/callback', 'HMAC-SHA1');
      console.log(cfg.RDB_ACCESS_TOKEN);
      console.log(cfg.RDB_ACCESS_TOKEN_SECRET);
    }

    Readability.prototype.getBookmarks = function(callback) {
      return this.oa.getProtectedResource('https://www.readability.com/api/rest/v1/bookmarks', 'GET', cfg.RDB_ACCESS_TOKEN, cfg.RDB_ACCESS_TOKEN_SECRET, function(error, data, response) {
        if (error) {
          return callback('Error: getting OAuth resource: ' + error);
        } else {
          return callback(data);
        }
      });
    };

    Readability.prototype.addBookmark = function(item, callback) {
      console.log(item);
      return this.oa.post('https://www.readability.com/api/rest/v1/bookmarks', cfg.RDB_ACCESS_TOKEN, cfg.RDB_ACCESS_TOKEN_SECRET, item, function(error, data, response) {
        if (error.statusCode = 409) {
          return callback(data);
        } else if (error) {
          console.log(error);
          return callback('Error: getting OAuth resource: ' + error);
        } else {
          return callback(data);
        }
      });
    };

    Readability.prototype.login = function(callback) {
      console.log('Getting OAuth Request Token');
      return this.oa.getOAuthRequestToken(function(error, oauth_token, oauth_token_secret, results) {
        if (error) {
          console.log('error :' + JSON.stringify(error));
        }
        return callback({
          oauth_token: oauth_token,
          oauth_token_secret: oauth_token_secret
        });
      });
    };

    Readability.prototype.handleCallback = function(oauth_token, oauth_token_secret, oauth_verifier, callback) {
      return this.oa.getOAuthAccessToken(oauth_token, oauth_token_secret, oauth_verifier, function(error, oauth_access_token, oauth_access_token_secret, response) {
        if (error) {
          console.log('error :' + JSON.stringify(error));
        }
        if (response === void 0) {
          console.log('error: ' + response);
        }
        return callback({
          oauth_access_token: oauth_access_token,
          oauth_access_token_secret: oauth_access_token_secret
        });
      });
    };

    return Readability;

  })();

}).call(this);
